[package]
name = "abc302"
version = "0.1.0"
edition = "2021"

# dependencies added to new project
[dependencies]
proconio = { version = '=0.3.6', features = ['derive'] }
num = '=0.2.1'

[profile.release]
lto = true
panic = 'abort'
