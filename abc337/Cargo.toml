[package]
name = "abc337"
version = "0.1.0"
edition = "2021"

# dependencies added to new project
[dependencies]
itertools = "0.12.0"
proconio = { version = "0.4.3", features = ["derive"] }
regex = "1.3.6"

[profile.release]
lto = true
panic = 'abort'
